# Current Feature

**Feature 26: Platform General Settings**
**Status:** `verified`

## Goal
Build a centralized configuration hub for the Platform Console (`/platform/settings`) to manage global variables, feature flags (maintenance mode, signup disable), integrations (API keys), and platform branding, backed by a singleton `platform_settings` database table.

## Status
All implementation steps completed and verified.
