'use client';

// Re-export BatchFormDrawer as BatchFormModal for backward compatibility
export { BatchFormDrawer as BatchFormModal } from './BatchFormDrawer';
export type { BatchFormDrawerProps as BatchFormModalProps } from './BatchFormDrawer';
